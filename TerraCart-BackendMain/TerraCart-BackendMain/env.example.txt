# Server Configuration
NODE_ENV=production
PORT=5001

# Database
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Security - CHANGE THESE IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
SIGNED_URL_SECRET=your-signed-url-secret-change-this-in-production

# CORS - Add your frontend URLs (comma-separated, no spaces)
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-admin.vercel.app

# File Uploads
ALLOW_PUBLIC_UPLOADS=false
MAX_FILE_SIZE=10

# Rate Limiting
DISABLE_RATE_LIMIT=false
RATE_LIMIT_API=500
RATE_LIMIT_LOGIN=10
RATE_LIMIT_UPLOAD=50

# Security Settings
FORCE_HTTPS=true
SESSION_TIMEOUT=60

# Optional Settings
ENABLE_REQUEST_LOGGING=false
ORDER_AUTO_RELEASE_MINUTES=0

# Firebase Admin (individual service-account fields)
# Keep FIREBASE_PRIVATE_KEY with escaped \n in one line.
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY_ID=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=
# Firebase Web Push (required for customer browser background notifications)
FIREBASE_WEB_API_KEY=
FIREBASE_WEB_APP_ID=
FIREBASE_WEB_MESSAGING_SENDER_ID=
FIREBASE_WEB_AUTH_DOMAIN=
FIREBASE_WEB_STORAGE_BUCKET=
FIREBASE_WEB_VAPID_KEY=

# Optional debug tracing (enable only while troubleshooting)
BACKEND_ENABLE_NOTIFICATION_DEBUG=false
BACKEND_ENABLE_SOCKET_TRACE=false

